import type { Metadata } from "next";
import { PurchaseRequestForm } from "@/components/forms/purchase-request-form";
import { PageHero } from "@/components/marketing/page-hero";
export const metadata:Metadata={title:"Solicitar licencia",description:"Inicia una solicitud de compra de Blue Cat mediante transferencia bancaria."};
export default async function BuyPage({searchParams}:{searchParams:Promise<{plan?:string;cloud?:string}>}){const search=await searchParams;const initialPlan=search.plan==="enterprise"?"enterprise":"pyme";return <><PageHero eyebrow="Solicitud comercial" title="Comienza con tus datos, no con una cuenta.">Registraremos tu solicitud y generaremos un enlace privado. Si el plan no tiene un precio publicado, primero recibirás una cotización verificable.</PageHero><section className="section"><div className="container form-shell"><PurchaseRequestForm initialPlan={initialPlan} initialCloud={search.cloud==="true"}/><aside className="summary-card"><h2>Qué ocurrirá</h2><ul><li>Registramos la solicitud.</li><li>Cotizamos el alcance cuando corresponda.</li><li>Habilitamos monto e instrucciones.</li><li>Informas la transferencia.</li><li>El equipo verifica el pago.</li><li>Tras aprobarlo continúa la emisión.</li></ul><p style={{color:"#8fabc4",fontSize:".8rem"}}>Nunca debes transferir sin ver un monto vigente dentro de tu enlace privado.</p></aside></div></section></>}
